apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-service
  namespace: helpdeskflow
  labels:
    app.kubernetes.io/name: ticket-service
    app.kubernetes.io/part-of: helpdeskflow
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: ticket-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ticket-service
        app.kubernetes.io/part-of: helpdeskflow
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: ticket-service
      initContainers:
        - name: wait-for-db
          image: busybox:1.36
          command:
            - sh
            - -c
            - until nc -z helpdeskflow-db 5432; do echo waiting for db; sleep 2; done
      containers:
        - name: ticket-service
          image: rafals12390/helpdeskflow-ticket-service:v0.2.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 4003
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: helpdeskflow-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: helpdeskflow-config
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: helpdeskflow-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: helpdeskflow-config
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: helpdeskflow-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: helpdeskflow-secrets
                  key: JWT_SECRET
            - name: JWT_EXPIRES_IN
              valueFrom:
                configMapKeyRef:
                  name: helpdeskflow-config
                  key: JWT_EXPIRES_IN
            - name: INTERNAL_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: helpdeskflow-secrets
                  key: INTERNAL_SERVICE_TOKEN
            - name: TICKET_SERVICE_PORT
              valueFrom:
                configMapKeyRef:
                  name: helpdeskflow-config
                  key: TICKET_SERVICE_PORT
            - name: NOTIFICATION_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: helpdeskflow-config
                  key: NOTIFICATION_SERVICE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 160Mi
            limits:
              cpu: 500m
              memory: 320Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: helpdeskflow
  labels:
    app.kubernetes.io/name: ticket-service
    app.kubernetes.io/part-of: helpdeskflow
spec:
  selector:
    app.kubernetes.io/name: ticket-service
  ports:
    - name: http
      port: 4003
      targetPort: http
  type: ClusterIP
