# Project for the CI/CD Process Security course

## Project Links

- GitHub repository: [rafals123/HelpDeskFlow-k8s](https://github.com/rafals123/HelpDeskFlow-k8s)
- Docker Hub - frontend: [rafals12390/helpdeskflow-frontend](https://hub.docker.com/r/rafals12390/helpdeskflow-frontend)
- Docker Hub - API Gateway: [rafals12390/helpdeskflow-api-gateway](https://hub.docker.com/r/rafals12390/helpdeskflow-api-gateway)
- Docker Hub - Auth Service: [rafals12390/helpdeskflow-auth-service](https://hub.docker.com/r/rafals12390/helpdeskflow-auth-service)
- Docker Hub - User Service: [rafals12390/helpdeskflow-user-service](https://hub.docker.com/r/rafals12390/helpdeskflow-user-service)
- Docker Hub - Ticket Service: [rafals12390/helpdeskflow-ticket-service](https://hub.docker.com/r/rafals12390/helpdeskflow-ticket-service)
- Docker Hub - Notification Service: [rafals12390/helpdeskflow-notification-service](https://hub.docker.com/r/rafals12390/helpdeskflow-notification-service)
- Docker Hub - Database: [rafals12390/helpdeskflow-db](https://hub.docker.com/r/rafals12390/helpdeskflow-db)

## Overview

![Project architecture](./image_doc/scheme.png)

HelpDeskFlow is a microservices-based IT help desk application prepared for the CI/CD and systems security course. The system allows end users to register and log in, create support cases, add comments, and follow status changes. IT staff can review incoming cases, assign support representatives, update statuses, and trigger email notifications.

The application is containerized with Docker Compose and uses a shared PostgreSQL database. The backend is split into focused services, while the frontend is built with Next.js and communicates with the backend through a single API Gateway.

## Microservices

![Microservices architecture generated with compose-viz](./image_doc/compose-viz.png)

- [Frontend](./frontend): Next.js user interface available on `http://localhost:3000`.
- [API Gateway](./services/api-gateway): Express-based entry point that proxies requests to `/api/auth`, `/api/users`, and `/api/cases`.
- [Auth Service](./services/auth-service): Registers users, verifies credentials, issues JWT tokens, and seeds default `admin` and `support` accounts.
- [User Service](./services/user-service): Returns the current user profile and exposes the list of support representatives.
- [Ticket Service](./services/ticket-service): Handles support cases, comments, assignee changes, status changes, and case history.
- [Notification Service](./services/notification-service): Internal-only service triggered by `ticket-service`. It sends status change emails through configurable SMTP or works in simulation mode if SMTP is not configured.
- **Database (PostgreSQL)**: Shared SQL database storing `users`, `cases`, `case_comments`, `case_history`, and `notifications`.
- [Database Image](./infra/db/Dockerfile): Local hardened Postgres image used by Docker Compose; it keeps the official init flow and replaces the vulnerable `gosu` helper with `su-exec`.
- **Mailpit**: Local SMTP inbox used to test outgoing emails in development.

All application services have custom Dockerfiles, and the current images were published to Docker Hub under the `rafals12390` namespace. The application images use the `v0.2.0` release line, while the frontend uses `v0.2.1` for the Kubernetes ingress build. The images support both `linux/amd64` and `linux/arm64` with SBOM attestation generated by `buildx`.

## Docker Image Security

The current public images used for the project are:

- `rafals12390/helpdeskflow-frontend:v0.2.1`
- `rafals12390/helpdeskflow-api-gateway:v0.2.0`
- `rafals12390/helpdeskflow-auth-service:v0.2.0`
- `rafals12390/helpdeskflow-user-service:v0.2.0`
- `rafals12390/helpdeskflow-ticket-service:v0.2.0`
- `rafals12390/helpdeskflow-notification-service:v0.2.0`
- `rafals12390/helpdeskflow-db:v0.2.0`

According to the final Docker Scout verification prepared for the first project stage, every image in the `v0.1.2` set reports `0C 0H`.

To reach this state, the final runtime images were pinned to `node:22.22.2-alpine`, unnecessary package manager binaries were removed from the runtime layers, and the frontend was converted to a static export served by a lightweight static server.

The PostgreSQL image needed an extra hardening step. The upstream `postgres:17-alpine3.22` image currently reports `1C 8H` in Docker Scout because of the bundled `gosu` binary compiled with a vulnerable Go standard library. In this repository, Docker Compose builds a local replacement image `helpdeskflow-db:local` from [infra/db/Dockerfile](./infra/db/Dockerfile), swaps `gosu` for `su-exec`, and keeps the official `/docker-entrypoint-initdb.d` initialization flow unchanged.

### Safety of Frontend image

![Docker Scout scanning results for frontend](./image_doc/frontend.png)

`rafals12390/helpdeskflow-frontend:v0.2.1` reports `No vulnerable package detected`.

### Safety of API Gateway image

![Docker Scout scanning results for API Gateway](./image_doc/api_gateway.png)

`rafals12390/helpdeskflow-api-gateway:v0.2.0` reports `No vulnerable package detected`.

### Safety of Auth Service image

![Docker Scout scanning results for Auth Service](./image_doc/auth_service.png)

`rafals12390/helpdeskflow-auth-service:v0.2.0` reports `No vulnerable package detected`.

### Safety of User Service image

![Docker Scout scanning results for User Service](./image_doc/user_service.png)

`rafals12390/helpdeskflow-user-service:v0.2.0` reports `No vulnerable package detected`.

### Safety of Ticket Service image

![Docker Scout scanning results for Ticket Service](./image_doc/ticker_service.png)

`rafals12390/helpdeskflow-ticket-service:v0.2.0` reports `No vulnerable package detected`.

### Safety of Notification Service image

![Docker Scout scanning results for Notification Service](./image_doc/notofication_service.png)

`rafals12390/helpdeskflow-notification-service:v0.2.0` reports `No vulnerable package detected`.

### Safety of Database image

![Docker Scout scanning results for Database](./image_doc/db.png)

The database image used by Docker Compose is `helpdeskflow-db:local`, built from [infra/db/Dockerfile](./infra/db/Dockerfile).

- upstream base image `postgres:17-alpine3.22`: `1C 8H 12M 2L 6?`
- local hardened image `helpdeskflow-db:local`: `0C 0H 2M 1L 1?`

The remaining findings after hardening are medium, low, and unspecified issues in Alpine packages that currently do not have upstream fixes according to Docker Scout.

## Getting Started

To get started with the project, follow the steps below:

1. Clone the repository:

   ```bash
   git clone https://github.com/rafals123/HelpDeskFlow-k8s.git
   cd HelpDeskFlow-k8s
   ```

2. Create the required environment files:

   ```bash
   cp .env.example .env
   cp frontend/.env.local.example frontend/.env.local
   ```

3. By default Docker Compose wires `notification-service` to Mailpit. After startup:

   - SMTP endpoint inside Compose: `mailpit:1025`
   - Mailpit web inbox: `http://localhost:8025`

   If you want to switch to simulation mode or real SMTP, adjust `.env`:

   ```env
   INTERNAL_SERVICE_TOKEN=change-me-internal-token
   NOTIFICATION_MODE=smtp
   SMTP_HOST=mailpit
   SMTP_PORT=1025
   SMTP_SECURE=false
   SMTP_FROM=HelpDeskFlow <noreply@helpdeskflow.local>
   SMTP_USER=
   SMTP_PASS=
   ```

   For simulation, set `NOTIFICATION_MODE=simulation`. For real SMTP, keep `NOTIFICATION_MODE=smtp` and replace `SMTP_HOST`, `SMTP_PORT`, and optional auth fields. If you run `notification-service` outside Docker Compose, use `SMTP_HOST=localhost` instead of `mailpit`.

4. Install dependencies:

   ```bash
   npm install
   ```

5. Launch the full stack with Docker Compose:

   ```bash
   npm run docker:up
   ```

6. The application will be available at:

   - frontend: `http://localhost:3000`
   - API Gateway: `http://localhost:8080`
   - auth-service: `http://localhost:4001`
   - user-service: `http://localhost:4002`
   - ticket-service: `http://localhost:4003`
   - PostgreSQL on the host: `localhost:5434`
   - Mailpit inbox: `http://localhost:8025`

## Default Accounts

The following accounts are created automatically on startup by `auth-service`:

- support representative:
  - email: `support@helpdeskflow.local`
  - password: `Support123!`
- administrator:
  - email: `admin@helpdeskflow.local`
  - password: `Admin123!`

A regular end user can register directly from the frontend.

## Useful Commands

```bash
npm run docker:logs
npm run docker:stop
npm run docker:down
npm run docker:publish
npm run docker:inspect
```

For local development outside Docker:

```bash
npm run dev
```

`notification-service` is no longer exposed through the public gateway. It is called internally by `ticket-service` with a shared internal token.

## Additional Documentation

- [docker-compose.yml](./docker-compose.yml)
- [infra/db/Dockerfile](./infra/db/Dockerfile)
